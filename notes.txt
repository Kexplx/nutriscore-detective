BARCODE_TO_NUTRISCORE_API = https://world.openfoodfacts.org/api/v0/product/5010034011922.json
IMG_TO_BARCODE_API = https://cool-artwork-234702.ew.r.appspot.com/img-to-barcode






<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <input id="fileUpload" type="file" />

    <script>
      const fileInput = document.getElementById("fileUpload");
      fileInput.onchange = () => {
        const img = fileInput.files[0];

        imageToBase64(img).then((imgAsBase64) =>
          fetch("https://cool-artwork-234702.ew.r.appspot.com/img-to-barcode", {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
            },
            body: JSON.stringify({ imgAsBase64 }),
          })
            .then((json) => json.json())
            .then((res) => console.log(res))
            .catch((err) => console.log(err))
        );
      };

      function imageToBase64(img) {
        return new Promise((resolve, reject) => {
          const reader = new FileReader();
          reader.readAsDataURL(img);

          reader.onload = () => resolve(reader.result);
          reader.onerror = (err) => reject(err);
        });
      }
    </script>
  </body>
</html>
